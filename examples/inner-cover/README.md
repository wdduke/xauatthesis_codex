# 中文内封 12 种对照示例

本目录用于对照官方 Word 模板中的中文内封。

命名规则：

```text
<degree>-<degree-type>-<advisor-mode>.tex
```

- `doctor-academic-*`：学博
- `doctor-professional-*`：专博
- `master-academic-*`：学硕
- `master-professional-*`：专硕
- `single`：普通导师
- `associate`：合作导师
- `team`：导师团队

每个 `.tex` 只输出一页中文内封，便于和官方模板逐项比对。

